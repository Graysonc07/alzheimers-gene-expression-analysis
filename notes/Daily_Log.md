
# Project Log: Day 1

Got the repo set up and cleaned it up a bit so everything isn’t a mess. Went through the dataset summary again just to actually understand what I’m working with instead of jumping straight in.

For the first run, I kept it simple and combined everything instead of splitting by brain region. So it’s basically just Alzheimer’s vs control overall (not worrying about HIP vs EC for now).

Ran the GEO2R analysis and got the first results table with differentially expressed genes. Skimmed through the top hits based on p-values and fold change — looks like there are some clear differences showing up.

Next step is to download the full results and start properly digging into which genes actually matter instead of just the top list.
